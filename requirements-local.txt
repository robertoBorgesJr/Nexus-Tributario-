# Dependências para desenvolvimento e testes locais (sem cluster Databricks).
# Use com .venv-local:
#   python -m venv .venv-local
#   .venv-local\Scripts\pip install -r requirements-local.txt
#
# NÃO misture com requirements-databricks.txt no mesmo venv.

# ── Geração de dados sintéticos ───────────────────────────────────────────────
Jinja2>=3.1
Faker>=26.0

# ── Upload para ADLS Gen2 ─────────────────────────────────────────────────────
azure-storage-blob>=12.20
azure-identity>=1.17

# ── Spark local ───────────────────────────────────────────────────────────────
# setuptools restaura distutils removido no Python 3.12+, exigido pelo PySpark.
pyspark>=3.5
setuptools

# ── dbt (adaptador Databricks) ───────────────────────────────────────────────
dbt-databricks>=1.8

# ── Utilitários ──────────────────────────────────────────────────────────────
# python-dotenv: carrega .env para variáveis de ambiente (inclusive DATABRICKS_*)
python-dotenv>=1.0
