# Dependências para execução via Databricks Connect + Serverless Compute.
# Use com .venv (venv de produção):
#   python -m venv .venv
#   .venv\Scripts\pip install -r requirements-databricks.txt
#
# Serverless via serverless_compute_id=auto requer databricks-connect>=15.1.
# Versões 14.x exigem cluster_id e não suportam serverless.
#
# NÃO misture com requirements-local.txt no mesmo venv.

# ── Geração de dados sintéticos ───────────────────────────────────────────────
Jinja2>=3.1
Faker>=26.0

# ── Upload para ADLS Gen2 ─────────────────────────────────────────────────────
azure-storage-blob>=12.20
azure-identity>=1.17

# ── Databricks Connect (substitui pyspark) ────────────────────────────────────
databricks-connect>=15.1
setuptools

# ── dbt (adaptador Databricks) ───────────────────────────────────────────────
dbt-databricks>=1.8

# ── Utilitários ──────────────────────────────────────────────────────────────
python-dotenv>=1.0
